apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: mule-ui-green
spec:
  replicas:
  template:
    metadata:
      labels:
        app: mule-ui
        color: green
    spec:
      containers:
      - image: vishalbiyani/mule-ui:latest
        name: mule-ui
        ports:
        - containerPort: 8080
          name: mule-ui
