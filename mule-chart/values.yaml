# Default values for test-chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
spec:
  replicaCount-frontend: 1
  replicaCount-backend: 1  
image:
  repository-frontend: vishalbiyani/mule-ui
  repository-backend: vishalbiyani/mule-app 
  tag-frontend: replaceTagName
  tag-backend: replaceTagName 
