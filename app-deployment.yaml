apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: mule-app
  labels:
    app: mule-app
spec:
  replicas: 3
  template:
    metadata:
      labels:
        app: mule-app
        color: blue
    spec:
      containers:
      - image: vishalbiyani/mule-app:latest
        name: mule-app
        ports:
        - containerPort: 8081
          name: mule-app
